/**
 * @file packages/db-schema/src/index.ts
 * @contents Package entry: re-exports schema types and tables.
 * @use Consumed as `@chirp/db-schema` from the API and tooling.
 */

export * from "./schema";
